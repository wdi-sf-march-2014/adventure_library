<h1> Welcome to Raphael's adventure library! </h1>

<p>Locally made adventures:</p>
<ul>

<%= render :partial => 'index_entry', :collection => @local_adventures %>
</ul>

<p> Adventures from other servers:</p>
<ul>

<%= render :partial => 'index_entry', :collection => @foreign_adventures.select{|a| a.start} %>
</ul>

<p> Adventures with no start page:</p>
<ul>

<%= render :partial => 'bad_index_entry', :collection => @foreign_adventures.select{|a| a.start.nil?} %>
</ul>
<p> Add your library for scraping:</p>
<%= form_for Library.new do |l| %>
  <%= l.text_field :url, :placeholder => "example.com", :class => "urlfield" %>
<% end %>