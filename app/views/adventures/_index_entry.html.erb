<li>
<%= link_to(index_entry.title,
           adventure_page_path(index_entry, index_entry.start)) %>
<% if index_entry.library_id %>
  from:
  <%= link_to(index_entry.library.url, index_entry.library.url) %>
<% end %>
</li>